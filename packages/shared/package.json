{
  "name": "@bigroom/shared",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm exec tsup src/index.ts --format esm,cjs --dts",
    "dev": "pnpm exec tsup src/index.ts --watch --format esm,cjs --dts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.6.3"
  },
  "exports": {
    ".": "./dist/index.js"
  }
}
